{
  "name": "webpack-hashing",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "cd ../dll && yarn build && cd ../app && yarn install && webpack"
  },
  "devDependencies": {
    "webpack": "1.12.14"
  },
  "dependencies": {
    "jquery": "^3.2.1"
  }
}
